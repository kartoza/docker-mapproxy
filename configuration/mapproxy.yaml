# #####################################################################
#                 MapProxy example configuration
# #####################################################################
#
# This is _not_ a runnable configuration, but it contains most
# available options in meaningful combinations.
#
# Use this file in addition to the documentation to see where and how
# things can be configured.


services:
  demo:
  wms:
    # only offer WMS 1.1.1
    versions: ['1.1.1']

    # supported SRS for this WMS
    srs: ['EPSG:4326', 'EPSG:3857']

    # force the layer extents (BBOX) to be displayed in this SRS
    bbox_srs: ['EPSG:4326']

    # limit the supported image formats.
    image_formats: ['image/jpeg', 'image/png', 'image/gif', 'image/GeoTIFF', 'image/tiff']

    
    # return an OGC service exception when one or more sources return errors
    # or no response at all (e.g. timeout)
    on_source_errors: raise

    # some WMS clients do not send all required parameters in feature info
    # requests, MapProxy ignores these errors unless you set strict to true.
    strict: true

    # list of feature info types the server should offer
    featureinfo_types: ['text', 'html', 'xml']

    md:
      # metadata used in capabilities documents
      title: Mapproxy demo map
      abstract: The map showing how QGIS Server interacts with Mapproxy.
      online_resource:
      contact:
        person: Kartoza
        position: GIS Manager
        organization:
        address:
        city:
        postcode:
        country: south Africa
        phone:
        email:
      # multiline strings are possible with the right indention
      access_constraints:
        Insert license and copyright information for this service.
      fees: 'None'

layers:
  # layer with minimal options
  - name: sa
    title: Western Cape Provincial Boundary
    sources: [boundary_cache]


  # layer with multiple sources
  - name: cities
    title: Western Cape cities
    sources: [city_cache]

    # layer with multiple sources
    - name: rivers
      title: Western Cape rivers
      sources: [river_cache]

caches:
  
  # mbtile cache:
  boundary_cache:
    sources: [boundary_wms]
    grids: [osm_grid]
    meta_size: [5,5]
    meta_buffer: 20
    concurrent_tile_creators: 2
    cache:
      type: mbtiles
      filename: boundary.mbtiles


  city_cache:
    sources: [city_wms]
    grids: [osm_grid]
    file_cache:
      cache:
        type: file
        directory: /mapproxy/city_cache
      format: custom_format

  river_cache:
    sources: [river_wms]
    grids: [osm_grid]
    meta_size: [5,5]
    meta_buffer: 20
    concurrent_tile_creators: 2
    cache:
      type: mbtiles
      filename: river.mbtiles

sources:
  boundary_wms:
    coverage:
      bbox: [16.3449768409, -34.8191663551, 32.830120477, -22.0913127581]
      bbox_srs: EPSG:4326
    type: wms
    req:
      url: http://localhost:8081/?map=/web/project.qgs
      layers: western_cape
      transparent: true
    supported_formats: [png]
    supported_srs: ['EPSG:3857', 'CRS:84']


  river_wms:
    coverage:
      bbox: [16.3449768409, -34.8191663551, 32.830120477, -22.0913127581]
      bbox_srs: EPSG:4326
    type: wms
    req:
      url: http://localhost:8081/?map=/web/project.qgs
      layers: rivers
      transparent: true
    supported_formats: [png]
    supported_srs: ['EPSG:3857', 'CRS:84']



  city_wms:
    coverage:
      bbox: [16.3449768409, -34.8191663551, 32.830120477, -22.0913127581]
      bbox_srs: EPSG:4326
    type: wms
    req:
      url: http://localhost:8081/?map=/web/project.qgs
      layers: city
      transparent: true
    supported_formats: [png]
    supported_srs: ['EPSG:3857', 'CRS:84']

grids:
  osm_grid:
    base: GLOBAL_MERCATOR
    srs: 'EPSG:3857'
    origin: nw 

globals:
  cache:
    meta_size: [6, 6]
    meta_buffer: 20
    lock_dir: './cache_data/locks'
    # where to store lockfiles for tile creation
    tile_lock_dir: './cache_data/tile_locks'