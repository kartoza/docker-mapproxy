{{- if .Values.nginx.enabled -}}
{{- $chartName := include "mapproxy.name" . -}}
{{- $releaseName := .Release.Name -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $releaseName }}-nginx-configmap
  labels:
    app: {{ $releaseName }}-{{ $chartName }}
    component: nginx-configmap
    environment: {{ .Values.environment }}
    release: {{ $releaseName }}
data:
  NGINX_CONFIG: {{ tpl (.Files.Get "config/default.conf") . | quote }}
  NGINX_ROOT_CONFIG: {{ .Files.Get "config/nginx.conf" | quote }}
  NGINX_MAPPROXY_CONFIG: {{ tpl (.Files.Get "config/nginx.mapproxy.conf") . | quote }}
  NGINX_MAPPROXY_WMTS_CONFIG: {{ tpl (.Files.Get "config/nginx.mapproxy.wmts.conf") . | quote }}
  {{- if and (not .Values.nginx.enabledOpala) (.Values.nginx.authorization.enabled) }}
  JWT_JS: {{ tpl (.Files.Get "config/jwt.js") . | quote }}
  {{- end }}
{{- end }}
